import { Typography } from "antd";

export default function DeliveriesPage() {
  return (
    <div>
      <Typography.Title level={4}>Deliveries</Typography.Title>
      <Typography.Text type="secondary">Coming in Week 3 — delivery commit + history.</Typography.Text>
    </div>
  );
}
