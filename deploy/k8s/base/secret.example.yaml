# Template only — copy to a local file (never commit real values), replace placeholders, then apply:
#
#   kubectl apply -n cyber-databrew-dev -f secret.local.yaml
#   kubectl apply -n cyber-databrew-prod -f secret.local.yaml
#
# Or imperatively:
#   # In-cluster dev deps (deploy/k8s/dev-deps): DB_HOST=postgres, DB_PORT=5432
#   kubectl create secret generic cyber-databrew-secrets -n cyber-databrew-dev \
#     --from-literal=DB_HOST=postgres --from-literal=DB_PORT=5432 \
#     --from-literal=DB_USER=... --from-literal=DB_PASSWORD=... \
#     --from-literal=DB_NAME=... --from-literal=DATABREW_TOKEN=...
#
# Keys must match backend/.env.example (same names as env vars).
apiVersion: v1
kind: Secret
metadata:
  name: cyber-databrew-secrets
  labels:
    app.kubernetes.io/name: cyber-databrew
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  DB_HOST: REPLACE_CLOUD_SQL_OR_POSTGRES_HOST
  DB_PORT: "5432"
  DB_USER: REPLACE_DB_USER
  DB_PASSWORD: REPLACE_DB_PASSWORD
  DB_NAME: REPLACE_DB_NAME
  DATABREW_TOKEN: REPLACE_PHASE0_STATIC_TOKEN
