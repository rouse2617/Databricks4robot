# kubectl apply -k deploy/k8s/base/
# Requires Secret cyber-databrew-secrets in the target namespace (see secret.example.yaml).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
