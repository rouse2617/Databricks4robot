{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://data-platform/schemas/events/action_deleted.v1.json",
  "title": "action_deleted.v1",
  "description": "Emitted when an `actions` row is soft-deleted. Written same-tx with the actions update; consumers re-read the seg's remaining actions and upsert the seg ES doc.",
  "type": "object",
  "additionalProperties": true,
  "required": ["action_id", "asset_id"],
  "properties": {
    "action_id": {
      "type": "string",
      "pattern": "^[0-9A-Za-z]{8}$"
    },
    "asset_id": {
      "type": "string",
      "pattern": "^[0-9A-Za-z]{8}$",
      "description": "Parent seg asset identifier."
    },
    "version": {
      "type": "integer",
      "description": "Version of the row at the time of soft delete."
    }
  }
}
