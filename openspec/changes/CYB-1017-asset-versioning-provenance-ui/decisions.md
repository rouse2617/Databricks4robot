# Decisions — CYB-1017

- User approved implementation after agreeing 1017 is next slice (2026-05-22).
- `lineage` in provenance reuses the same JSON shape as `GET /assets/{id}/lineage` (no new lineage query).
- SDK deferred; OpenAPI + api-guide + smoke required in this PR.
