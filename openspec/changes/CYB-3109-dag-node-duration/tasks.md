# Tasks — CYB-3109

- [x] `WorkflowDagNode`: compute `formatDuration(startedAt, finishedAt)`; show `耗时 · 相对时间` on the card.
- [x] Enrich hover tooltip → 开始 / 完成 / 耗时 (absolute + duration).
- [x] biome clean; `WorkflowDagView.test.tsx` 5/5; `npm run build` clean.
- [ ] Deploy frontend dev + Chrome DevTools MCP: DAG node shows per-step duration + tooltip.
- [ ] PR to dev.
