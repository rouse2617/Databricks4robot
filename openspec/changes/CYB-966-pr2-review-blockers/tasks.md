# Tasks

- [x] Remove `assets.status` from `AssetRepo` SQL paths
- [x] Map filter field `status` → `lifecycle_state` with legacy value translation
- [x] Disable admin/internal routes in production without `ADMIN_TOKEN`
- [x] Renumber `018_add_saved_queries` → `026`, `023_mcap_summary_index_state` → `027`
- [x] CI: migration filename check + fresh Postgres + `TestFreshDB_AssetCRUD`
