# CYB-3826 Tasks

- [x] Linear CYB-3826(In Progress)
- [x] OpenSpec proposal
- [x] `PoolManager.tsx` 调度配置列:tolerations/nodeSelector 从 sched 和 rd 两处、多别名读
- [x] `pipelineApi.ts` `TargetScheduling` 类型补齐(nodeSelector / nodeSelectors / templateNodeSelector / templateTolerations)
- [x] Tier L(`tsc --noEmit`=0 + `npm run build` ✓)
- [ ] Dev 部署后 Chrome DevTools MCP 验(合并后)
- [ ] PR 到 dev(停在 open)
