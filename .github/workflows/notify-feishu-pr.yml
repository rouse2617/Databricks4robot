# Feishu on PRs targeting main → A 群 (FEISHU_BOT_WEBHOOK_MAIN).
name: Feishu PR Notifications

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed
    branches:
      - main

# Jobs disabled — no longer auto-commenting prod deploy commands on merge.
# Prod deployment is now triggered manually via other channels.
jobs: {}
