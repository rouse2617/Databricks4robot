# Disabled: push notifications were spamming Feishu group.
# CI result notifications are handled by notify-feishu-ci.yml (main branch, success only).
name: Feishu Push Notifications (disabled)

on:
  workflow_dispatch:
